# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kids_brand_ai

# Set ke "true" untuk memakai penyimpanan in-memory (tanpa PostgreSQL) — berguna untuk demo cepat
USE_IN_MEMORY=false

# LLM (untuk modul Content Generator & CS Bot nanti — provider agnostic)
LLM_PROVIDER=openai
LLM_API_KEY=
